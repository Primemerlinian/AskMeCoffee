<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <div>
    <img 
      class="profile-avatar"
      src="<%= profile.avatar %>" 
      alt="<%= profile.name %>'s avatar"
    >
  </div>
  <h1>It's <%= isSelf ? "you" : profile.name %>!</h1>
    <!-- <h2>
      <%= isSelf ? "You have" : `${profile.name} has` %> drinks! Here they are!
    </h2>
    <h2>
      <%= isSelf ? "You don't" : `${profile.name} doesn't` %> have any drinks.
    </h2>  --> 
</main>

<%- include('../partials/footer') %>